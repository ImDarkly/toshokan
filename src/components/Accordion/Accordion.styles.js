import styled from 'styled-components';

export const StyledAccordion = styled.ul`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    flex: 1 0 0;
    border-radius: 1rem;
    min-height: 3rem;
    margin: 1rem 1rem;
    padding: 0;
    list-style: none;
    overflow: hidden;
`