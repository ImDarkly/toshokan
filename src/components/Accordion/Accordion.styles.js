import styled from 'styled-components';

export const StyledAccordion = styled.ul`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    flex: 1 0 0;
    height: 100%;
    margin: 0 1rem;
    padding: 0;
    padding-bottom: 1rem;
    list-style: none;
    overflow-y: auto;
`